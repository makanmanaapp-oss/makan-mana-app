# Home hero asset slot — Front Page Redesign 1A

Slot for the Home hero food visual referenced by the redesign.

**Pending:** a licensed / project-owned food hero image (recommended
`home_food_hero.png`, transparent PNG, ~1000×1000, subject centred).

Until the final photographic asset is dropped here and wired, the Home hero
degrades gracefully to a decorative red circle + MakanMana food glyph
(`_HeroFoodVisual` in `home_screen.dart`) — no low-quality screenshot crop is
used. Drop the asset here and reference it via `Image.asset` in the hero.
