{
  "//": "Phase 1.2 + Control Center bridge tests. Compiles domain and read-only bridge tests to lib-test/ without changing production output.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "lib-test",
    "rootDir": "src",
    "noUnusedLocals": false,
    "sourceMap": false
  },
  "include": ["src/domain", "src/controlCenter"],
  "exclude": ["node_modules"]
}
