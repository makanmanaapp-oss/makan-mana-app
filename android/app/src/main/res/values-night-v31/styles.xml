<?xml version="1.0" encoding="utf-8"?>
<!-- Splash Redesign 2B (Option B) — Android 12+ system splash, Dark mode.
     Launch identity stays red across themes; only system icon brightness may
     differ. windowSplashScreenBackground must be set explicitly on API 31+. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
